datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

generator client {
  provider = "prisma-client-js"
}

// The exact schema appears to be irrelevant
model User {
  id Int @id @default(autoincrement())
}
